Analizar la estructura de la tabla `personal_sistema` y todas sus asociaciones para identificar cada campo que utiliza. Revisar también el código fuente en `backend/src/` para determinar qué campos se referencian realmente en controladores, servicios y tipos. Eliminar aquellos campos que ya no se usan ni en la BD ni en el código. Agregar el campo `fecha_nacimiento` (tipo `DATE`) para el control de cumpleañeros del personal, verificando antes que no exista ya en la tabla `personas` (relacionada con `personal_sistema`) para evitar duplicidad. Todos los cambios se harán exclusivamente sobre la base de datos local por el momento, nunca en Neon. Se debe crear un archivo de migración SQL (ej. `migracion_v6_limpieza_personal.sql`) en `backend/` para que los cambios sean versionados y reproducibles. Se generará un dump local de respaldo (`pg_dump`) antes de iniciar los cambios para poder revertir si algo sale mal. Una vez aplicada la migración, actualizar las interfaces y tipos TypeScript que reflejen los campos modificados.